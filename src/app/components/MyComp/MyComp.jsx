import React from 'react';
import PropTypes from 'prop-types';

// * components

// * style
import './MyComp.scss';

// * scripts

// * img

// * icons

const MyComp = ({ prop }) => {
   return (
      <div className="MyComp">

      </div>
   );
};

MyComp.propTypes = {
   prop: PropTypes,
};

export default MyComp;
